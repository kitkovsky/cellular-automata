import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Delightful Animations | Seeds',
}

export default function SeedsLayout({ children }: { children: JSX.Element }) {
  return children
}
